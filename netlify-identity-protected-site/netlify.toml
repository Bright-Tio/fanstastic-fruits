[build]
  publish = "public"
  functions = "netlify/functions"

# Optional: keep the default Identity widget route working nicely
[[redirects]]
  from = "/.netlify/identity/*"
  to = "/.netlify/identity/:splat"
  status = 200
